Diff in /data/projects/frankenscipy/crates/fsci-linalg/src/lib.rs:5704:
     if n == 0 {
         return Ok(Vec::new());
     }
[31m-    let is_symmetric = (0..n).all(|i| {
[m[31m-        (0..n)
[m[31m-            .all(|j| (m[(i, j)] - m[(j, i)]).abs() < 1e-12 * m[(i, j)].abs().max(1.0))
[m[31m-    });
[m[32m+    let is_symmetric = (0..n)
[m[32m+        .all(|i| (0..n).all(|j| (m[(i, j)] - m[(j, i)]).abs() < 1e-12 * m[(i, j)].abs().max(1.0)));
[m     if is_symmetric {
         return funm(a, |x| if x >= 0.0 { 1.0 } else { -1.0 }, options);
     }
